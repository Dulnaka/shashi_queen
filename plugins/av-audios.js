let handler = m => m
handler.all = async function (m) {

    if (/^.alive$/i.test(m.text) ) {
      let av = './src/mp3/Shashi.mp3'
      this.sendPresenceUpdate('recording', m.chat);
      this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
      }

  if (/^.menu$/i.test(m.text) ) {
    let av = './src/mp3/Shashi.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }

 if (/^adaraya$/i.test(m.text) ) {
    let av = './src/mp3/adaraya.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }

 if (/^ආදරය$/i.test(m.text) ) {
    let av = './src/mp3/adaraya.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }

 if (/^Adaraya$/i.test(m.text) ) {
    let av = './src/mp3/adaraya.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }

 if (/^බඩුව$/i.test(m.text) ) {
    let av = './src/mp3/baduwa.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }

 if (/^බඩුවක්$/i.test(m.text) ) {
    let av = './src/mp3/baduwa.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }

 if (/^Baduwa$/i.test(m.text) ) {
    let av = './src/mp3/baduwa.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }

 if (/^baduwa$/i.test(m.text) ) {
    let av = './src/mp3/baduwa.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }


 if (/^Baduwak$/i.test(m.text) ) {
    let av = './src/mp3/baduwa.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }

 if (/^ගොන් බඩුව$/i.test(m.text) ) {
    let av = './src/mp3/baduwa.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }

 if (/^කැරි බඩුව$/i.test(m.text) ) {
    let av = './src/mp3/baduwa.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }

 if (/^බයයි$/i.test(m.text) ) {
    let av = './src/mp3/baya.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }

 if (/^Bayayi$/i.test(m.text) ) {
    let av = './src/mp3/baya.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }

 if (/^bayayi$/i.test(m.text) ) {
    let av = './src/mp3/baya.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }

 if (/^බොක්ක$/i.test(m.text) ) {
    let av = './src/mp3/bokka.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }

 if (/^Bokka$/i.test(m.text) ) {
    let av = './src/mp3/bokka.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }
  
 if (/^bokka$/i.test(m.text) ) {
    let av = './src/mp3/bokka.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }

 if (/^බායි$/i.test(m.text) ) {
    let av = './src/mp3/by.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }

 if (/^By$/i.test(m.text) ) {
    let av = './src/mp3/by.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }

 if (/^by$/i.test(m.text) ) {
    let av = './src/mp3/by.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }
    
 if (/^bye$/i.test(m.text) ) {
    let av = './src/mp3/by.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }
    
 if (/^Bye$/i.test(m.text) ) {
    let av = './src/mp3/by.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }
 
 if (/^චන්ඩිද$/i.test(m.text) ) {
    let av = './src/mp3/chandida.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }

 if (/^Chandida$/i.test(m.text) ) {
    let av = './src/mp3/chandida.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }

  if (/^chandida$/i.test(m.text) ) {
    let av = './src/mp3/chandida.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }   

 if (/^මොකෝ$/i.test(m.text) ) {
    let av = './src/mp3/g.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }    

 if (/^Moko$/i.test(m.text) ) {
    let av = './src/mp3/g.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }  

 if (/^moko$/i.test(m.text) ) {
    let av = './src/mp3/g.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }    

 if (/^Gn$/i.test(m.text) ) {
    let av = './src/mp3/Gn.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }   

 if (/^gn$/i.test(m.text) ) {
    let av = './src/mp3/Gn.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }    

if (/^ගුඩ් නයිට්$/i.test(m.text) ) {
    let av = './src/mp3/Gn.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }       

if (/^good night$/i.test(m.text) ) {
    let av = './src/mp3/Gn.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }       

if (/^Good night$/i.test(m.text) ) {
    let av = './src/mp3/Gn.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }   

if (/^Hi$/i.test(m.text) ) {
    let av = './src/mp3/hi.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }   

if (/^hi$/i.test(m.text) ) {
    let av = './src/mp3/hi.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }   

if (/^හායි$/i.test(m.text) ) {
    let av = './src/mp3/hi.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }       

if (/^Hmm$/i.test(m.text) ) {
    let av = './src/mp3/hmm.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }      

if (/^hmm$/i.test(m.text) ) {
    let av = './src/mp3/hmm.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }      

if (/^Hum$/i.test(m.text) ) {
    let av = './src/mp3/hmm.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }         

if (/^hum$/i.test(m.text) ) {
    let av = './src/mp3/hmm.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }      

if (/^Mmm$/i.test(m.text) ) {
    let av = './src/mp3/hmm.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }     

if (/^mmm$/i.test(m.text) ) {
    let av = './src/mp3/hmm.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }         

if (/^Hm$/i.test(m.text) ) {
    let av = './src/mp3/hmm.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }      

if (/^hm$/i.test(m.text) ) {
    let av = './src/mp3/hmm.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }      

if (/^mm$/i.test(m.text) ) {
    let av = './src/mp3/hmm.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }      

if (/^Mm$/i.test(m.text) ) {
    let av = './src/mp3/hmm.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }      

if (/^හුම්$/i.test(m.text) ) {
    let av = './src/mp3/hmm.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }     

if (/^ම්ම්$/i.test(m.text) ) {
    let av = './src/mp3/hmm.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }           

if (/^හූ$/i.test(m.text) ) {
    let av = './src/mp3/hu.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }         

if (/^හුත්ති$/i.test(m.text) ) {
    let av = './src/mp3/hutti.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }      

if (/^වේසි$/i.test(m.text) ) {
    let av = './src/mp3/hutti.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }          

if (/^බැල්ලි$/i.test(m.text) ) {
    let av = './src/mp3/hutti.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }      

if (/^කැරි වේසි$/i.test(m.text) ) {
    let av = './src/mp3/hutti.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }      

if (/^Huththi$/i.test(m.text) ) {
    let av = './src/mp3/hutti.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }      

if (/^huththi$/i.test(m.text) ) {
    let av = './src/mp3/hutti.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }      

if (/^Hutti$/i.test(m.text) ) {
    let av = './src/mp3/hutti.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }      

if (/^hutti$/i.test(m.text) ) {
    let av = './src/mp3/hutti.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }          

if (/^wesi$/i.test(m.text) ) {
    let av = './src/mp3/hutti.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }      

if (/^Wesi$/i.test(m.text) ) {
    let av = './src/mp3/hutti.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }      

if (/^Balli$/i.test(m.text) ) {
    let av = './src/mp3/hutti.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }          

if (/^balli$/i.test(m.text) ) {
    let av = './src/mp3/hutti.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }      

if (/^I love you$/i.test(m.text) ) {
    let av = './src/mp3/iloveyou.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }      

if (/^i love you$/i.test(m.text) ) {
    let av = './src/mp3/iloveyou.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }          

if (/^අයි ලවු යූ$/i.test(m.text) ) {
    let av = './src/mp3/iloveyou.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }          

if (/^කන්න$/i.test(m.text) ) {
    let av = './src/mp3/kanna.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }         

if (/^Kanna$/i.test(m.text) ) {
    let av = './src/mp3/kanna.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }       

if (/^kanna$/i.test(m.text) ) {
    let av = './src/mp3/kanna.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }      

if (/^ලස්සනයි$/i.test(m.text) ) {
    let av = './src/mp3/lassana.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }      

if (/^Lassanayi$/i.test(m.text) ) {
    let av = './src/mp3/lassana.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }          

if (/^lassana$/i.test(m.text) ) {
    let av = './src/mp3/lassana.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }          

if (/^ලොකු සීන් නෙ$/i.test(m.text) ) {
    let av = './src/mp3/lseen.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^Loku seen ne$/i.test(m.text) ) {
    let av = './src/mp3/lseen.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }      

if (/^loku seen ne$/i.test(m.text) ) {
    let av = './src/mp3/lseen.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }          

if (/^ඔයා මගේ$/i.test(m.text) ) {
    let av = './src/mp3/mage.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }    

if (/^ඒකි මගේ$/i.test(m.text) ) {
    let av = './src/mp3/mage.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }      

if (/^මගේ විතරයි$/i.test(m.text) ) {
    let av = './src/mp3/mage.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^ඔයා මගේ$/i.test(m.text) ) {
    let av = './src/mp3/mage.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }    

if (/^Oya mage$/i.test(m.text) ) {
    let av = './src/mp3/mage.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }    

if (/^Mk$/i.test(m.text) ) {
    let av = './src/mp3/mk.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }    


if (/^mk$/i.test(m.text) ) {
    let av = './src/mp3/mk.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }    


if (/^ඔයා කෙල්ලෙක්ද$/i.test(m.text) ) {
    let av = './src/mp3/moda.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^Oya kellekda$/i.test(m.text) ) {
    let av = './src/mp3/moda.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^oya kellekda$/i.test(m.text) ) {
    let av = './src/mp3/moda.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^Na$/i.test(m.text) ) {
    let av = './src/mp3/na.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^na$/i.test(m.text) ) {
    let av = './src/mp3/na.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }            

if (/^නෑ$/i.test(m.text) ) {
    let av = './src/mp3/na.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^නැ$/i.test(m.text) ) {
    let av = './src/mp3/na.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^නටපන්$/i.test(m.text) ) {
    let av = './src/mp3/natam.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^Natapan$/i.test(m.text) ) {
    let av = './src/mp3/natam.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }           

if (/^natapan$/i.test(m.text) ) {
    let av = './src/mp3/natama.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^අඩෝ$/i.test(m.text) ) {
    let av = './src/mp3/oyi.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^Ado$/i.test(m.text) ) {
    let av = './src/mp3/oyi.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^ado$/i.test(m.text) ) {
    let av = './src/mp3/oyi.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }           

if (/^ඕයි$/i.test(m.text) ) {
    let av = './src/mp3/oyi.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^ගදයි$/i.test(m.text) ) {
    let av = './src/mp3/pade.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^Gadayi$/i.test(m.text) ) {
    let av = './src/mp3/pade.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^gadayi$/i.test(m.text) ) {
    let av = './src/mp3/pade.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }            

if (/^පඩයක් අරින්න$/i.test(m.text) ) {
    let av = './src/mp3/pade.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }            

if (/^Puk$/i.test(m.text) ) {
    let av = './src/mp3/pade.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }            

if (/^Huththa$/i.test(m.text) ) {
    let av = './src/mp3/pakaya.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^huththa$/i.test(m.text) ) {
    let av = './src/mp3/pakaya.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^හුත්තා$/i.test(m.text) ) {
    let av = './src/mp3/pakaya.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

 if (/^පකයා$/i.test(m.text) ) {
    let av = './src/mp3/pakaya.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }          

if (/^පයියා$/i.test(m.text) ) {
    let av = './src/mp3/pakaya.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }         

if (/^පොන්නයා$/i.test(m.text) ) {
    let av = './src/mp3/pakaya.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }            

if (/^කැරියා$/i.test(m.text) ) {
    let av = './src/mp3/pakaya.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^බල්ලා$/i.test(m.text) ) {
    let av = './src/mp3/pakaya.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^Pakaya$/i.test(m.text) ) {
    let av = './src/mp3/pakaya.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }           

 if (/^pakaya$/i.test(m.text) ) {
    let av = './src/mp3/pakaya.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }           

 if (/^Ponnaya$/i.test(m.text) ) {
    let av = './src/mp3/pakaya.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }           

 if (/^ponnaya$/i.test(m.text) ) {
    let av = './src/mp3/pakaya.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }           

 if (/^Kariya$/i.test(m.text) ) {
    let av = './src/mp3/pakaya.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }           

 if (/^kariya$/i.test(m.text) ) {
    let av = './src/mp3/pakaya.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }           

if (/^Balla$/i.test(m.text) ) {
    let av = './src/mp3/pakaya.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }            

if (/^balla$/i.test(m.text) ) {
    let av = './src/mp3/pakaya.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }           

if (/^Bijja$/i.test(m.text) ) {
    let av = './src/mp3/pakaya.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }            

if (/^පලයන්$/i.test(m.text) ) {
    let av = './src/mp3/palayan.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }            

 if (/^Palayan$/i.test(m.text) ) {
    let av = './src/mp3/palayan.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }           

if (/^palayan$/i.test(m.text) ) {
    let av = './src/mp3/palayan.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^කුක්කු$/i.test(m.text) ) {
    let av = './src/mp3/pissu.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^kukku$/i.test(m.text) ) {
    let av = './src/mp3/pissu.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^Kukku$/i.test(m.text) ) {
    let av = './src/mp3/pissu.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^ඒකි මට ඕන$/i.test(m.text) ) {
    let av = './src/mp3/pk.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^මන් ඒකි ආදරෙයි$/i.test(m.text) ) {
    let av = './src/mp3/pk.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }            

if (/^ඒකි දෙන්නෑ$/i.test(m.text) ) {
    let av = './src/mp3/pk.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }            

if (/^දෝණිගෙන් ඈත්වෙයන්$/i.test(m.text) ) {
    let av = './src/mp3/pk.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }           

if (/^Doni case$/i.test(m.text) ) {
    let av = './src/mp3/pk.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

 if (/^doni case$/i.test(m.text) ) {
    let av = './src/mp3/pk.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }           

if (/^Eki mata ona$/i.test(m.text) ) {
    let av = './src/mp3/pk.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^Oya mage$/i.test(m.text) ) {
    let av = './src/mp3/pk.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^Doni mage$/i.test(m.text) ) {
    let av = './src/mp3/pk.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }            

if (/^Umma$/i.test(m.text) ) {
    let av = './src/mp3/umma.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }            

if (/^umma$/i.test(m.text) ) {
    let av = './src/mp3/umma.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }            

 if (/^උම්මා$/i.test(m.text) ) {
    let av = './src/mp3/umma.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }      

if (/^Doni$/i.test(m.text) ) {
    let av = './src/mp3/umma.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }     

if (/^doni$/i.test(m.text) ) {
    let av = './src/mp3/umma.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }               

if (/^දෝණි$/i.test(m.text) ) {
    let av = './src/mp3/umma.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^කවුද$/i.test(m.text) ) {
    let av = './src/mp3/umma.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^kwd$/i.test(m.text) ) {
    let av = './src/mp3/umma.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^Kwd$/i.test(m.text) ) {
    let av = './src/mp3/umma.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }        

if (/^O$/i.test(m.text) ) {
    let av = './src/mp3/umma.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }           

if (/^Xxx$/i.test(m.text) ) {
    let av = './src/mp3/xxx.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }           

if (/^xxx$/i.test(m.text) ) {
    let av = './src/mp3/xxx.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }           

if (/^Sex$/i.test(m.text) ) {
    let av = './src/mp3/xxx.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }               

if (/^sex$/i.test(m.text) ) {
    let av = './src/mp3/xxx.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }           

if (/^සෙක්ස්$/i.test(m.text) ) {
    let av = './src/mp3/xxx.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }           

if (/^මන් යනෝ$/i.test(m.text) ) {
    let av = './src/mp3/yano.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }         

if (/^අපි යනෝ$/i.test(m.text) ) {
    let av = './src/mp3/yano.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }           

if (/^Man yani$/i.test(m.text) ) {
    let av = './src/mp3/yano.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }         

if (/^Api yano$/i.test(m.text) ) {
    let av = './src/mp3/yano.mp3'
    this.sendPresenceUpdate('recording', m.chat);
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }         


 if (/^bgm$/i.test(m.text) ) {
    let av = './src/mp3/bgm.mp3'
    this.sendFile(m.chat, av, 'audio.mp3', null, m, true, { type: 'audioMessage', ptt: true })
   }
  
return ! 
 }
 
export default handler
